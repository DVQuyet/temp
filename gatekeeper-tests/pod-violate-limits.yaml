apiVersion: v1
kind: Pod
metadata:
  name: test-violate-limits
  namespace: default
spec:
  containers:
  - name: nginx
    image: nginx:1.25.1
    # Vi phạm: thiếu phần resources.limits
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
