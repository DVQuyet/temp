apiVersion: v1
kind: Pod
metadata:
  name: test-violate-root
  namespace: default
spec:
  securityContext:
    runAsUser: 0  # Vi phạm: chạy với quyền root (runAsUser: 0)
  containers:
  - name: nginx
    image: nginx:1.25.1
    resources:
      limits:
        cpu: 100m
        memory: 128Mi
