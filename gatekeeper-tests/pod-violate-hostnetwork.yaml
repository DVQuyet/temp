apiVersion: v1
kind: Pod
metadata:
  name: test-violate-hostnetwork
  namespace: default
spec:
  hostNetwork: true  # Vi phạm: dùng mạng host của Node
  containers:
  - name: nginx
    image: nginx:1.25.1
    resources:
      limits:
        cpu: 100m
        memory: 128Mi
